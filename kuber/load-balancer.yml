apiVersion: v1
kind: Service
metadata:
  name: vrwww-load-balancer
  annotations:
    service.beta.kubernetes.io/do-loadbalancer-certificate-id: ""
spec:
  type: LoadBalancer
  selector:
    app: vrww
  ports:
    - protocol: TCP
      port: 443
      targetPort: 80
      name: https
